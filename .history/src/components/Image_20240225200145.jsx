import product from 'src/product.jsx'

const Image = () => {
    const [image, setImage] = useState(product.imageURL);
    const [index, setIndex] = use
  return (
    <div>
      
    </div>
  )
}

export default Image
