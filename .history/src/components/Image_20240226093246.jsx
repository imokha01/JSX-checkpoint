import products from "../product";
import { useState } from "react";

const Image = () => {
    const [image, setImage] = useState(products.imageURL);

  return (
    <div>
      
    </div>
  )
}

export default Image
